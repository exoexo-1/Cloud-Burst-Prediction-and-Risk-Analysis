# HydroPrognosis – Cloudburst Prediction and Flood Risk Analysis

> Real-time flood vulnerability assessment and cloudburst prediction for Uttarakhand, powered by fuzzy logic, machine learning, and RAG-augmented LLM analysis.

**Live:** https://hydroprognosis.vercel.app

---

## What It Does

HydroPrognosis lets anyone click a point on a map and instantly get:

- A **Flood Vulnerability Index (FVI)** score (0–100) computed in real time using fuzzy logic
- A **cloudburst probability** (%) predicted by a trained ML model
- A full **risk analysis report** generated by GPT-4o-mini, grounded in a district-level knowledge base via RAG
- A **72-hour forecast** with safety recommendations for residents and authorities

The system is designed for Uttarakhand — a region that experiences frequent and deadly cloudbursts — and covers all 13 districts with district-specific knowledge bases.

---

## Architecture

```
User clicks map
      │
      ▼
Leaflet Frontend (React)
      │
      ├──► /fvi API  ──► FVI_Fuzzy.py
      │                   ├── Open-Meteo (live weather)
      │                   ├── Open-Elevation (terrain)
      │                   ├── OSMnx (hydrology/water proximity)
      │                   └── Fuzzy Logic Engine (scikit-fuzzy)
      │
      └──► /analysis API ──► LLM.py
                              ├── ML Regressor (cloudburst probability)
                              ├── RAG Service (ChromaDB + OpenAI Embeddings)
                              └── GPT-4o-mini (risk report generation)
```
<img width="492" height="407" alt="Screenshot 2025-08-12 225542" src="https://github.com/user-attachments/assets/59b187ee-5075-43b8-8eb2-4311435dfa64" />


---

## Tech Stack

| Layer | Tools |
|---|---|
| Frontend | React.js, Leaflet.js, Nominatim (reverse geocoding) |
| Backend | FastAPI, Python |
| FVI Engine | scikit-fuzzy, NumPy, OSMnx, GeoPandas, Shapely |
| Live Data | Open-Meteo API, Open-Elevation API, OpenStreetMap |
| ML Model | XGBoost / Random Forest regressor (14-feature vector), scikit-learn |
| RAG Pipeline | LangChain, ChromaDB, OpenAI Embeddings |
| LLM | GPT-4o-mini |
| Deployment | Vercel (frontend), Render (backend) |

---

## How FVI Is Computed

The **Flood Vulnerability Index** is calculated using a fuzzy logic system with 6 input variables:

| Input | Source |
|---|---|
| Rainfall (mm) | Open-Meteo live forecast |
| Slope (degrees) | Open-Elevation (sampled nearby points) |
| Imperviousness (%) | Derived from urbanization + socioeconomic data |
| Distance to water (m) | OSMnx waterway/water feature query |
| Soil moisture | Open-Meteo hourly data |
| Elevation (m) | Open-Elevation API |

Membership functions and fuzzy rules map these inputs to an FVI score (0–100), which is then categorised as Very Low / Low / Moderate / High / Very High. A fallback linear scoring function handles cases where the fuzzy system cannot converge.

---

## How Cloudburst Probability Is Predicted

A pre-trained regression model (`cloudburst_regressor.pkl`) predicts cloudburst probability from a **14-feature vector** built from live and historical weather data:

- Min/max temperature, humidity, pressure
- Precipitation sum, rain sum, precipitation probability
- Cloud cover, wind speed, wind gusts, wind direction (cardinal encoded)
- Is-day flag, temperature at 2m, weather code (encoded)

Previous week averages are fetched from Open-Meteo Archive API and blended with current conditions to capture antecedent moisture — a key cloudburst risk factor.

---

## RAG Knowledge Base

The `Knowledge Base/` directory contains structured markdown documents for each of Uttarakhand's 13 districts, covering:

- Risk factors and vulnerability profiles
- Historical cloudburst and flood events (1970–2025)
- Topography, geology, and hydrology
- Microclimate and terrain-specific patterns

These documents are embedded using OpenAI Embeddings and stored in ChromaDB. At query time, 4 targeted queries per district retrieve the most relevant context, which is passed to GPT-4o-mini alongside the FVI data and ML prediction.

---

## Project Structure

```
Cloud-Burst-Prediction-and-Risk-Analysis/
├── APIs/
│   ├── Apis.py              # FastAPI endpoints (/fvi, /analysis)
│   ├── FVI_Fuzzy.py         # Flood Vulnerability Index engine
│   ├── LLM.py               # RAG service + ML prediction + GPT report
│   ├── cloudburst_regressor.pkl
│   ├── scaler.pkl
│   └── vector_db/           # ChromaDB embeddings
├── Frontend-Map/
│   └── leaflet-map/         # React + Leaflet frontend
│       └── src/
│           ├── Map.js        # Interactive map, FVI fetch, marker tooltip
│           ├── FVIDetailsPanel.js
│           ├── homePage.js
│           └── ...
├── Knowledge Base/           # District-level markdown docs (13 districts)
├── RAG.ipynb                 # RAG pipeline notebook
└── requirements.txt
```

---

## API Reference

### `GET /fvi`
Computes Flood Vulnerability Index for a coordinate.

**Query params:** `lat`, `lon`, `district` (optional)

**Response:**
```json
{
  "fvi_score": 62.4,
  "risk_level": "Moderate",
  "key_factors": ["Moderate rainfall (34.2 mm)", "Close to water body (1200 m)"],
  "inputs": { "weather": {...}, "terrain": {...}, "hydrology": {...} }
}
```

### `POST /analysis`
Generates a full risk report using RAG + ML + GPT.

**Body:**
```json
{
  "place_name": "Rudraprayag",
  "fvi_data": { ... }
}
```

**Response:**
```json
{
  "analysis": "### 1. Flood Risk Level\nHigh...",
  "rag_context": "CONTEXT FOR RUDRAPRAYAG:...",
  "status": "success"
}
```

---

## Setup

```bash
# Clone
git clone https://github.com/exoexo-1/Cloud-Burst-Prediction-and-Risk-Analysis
cd Cloud-Burst-Prediction-and-Risk-Analysis

# Backend
pip install -r requirements.txt
# Add OPENAI_API_KEY to .env
uvicorn APIs.Apis:app --reload

# Frontend
cd Frontend-Map/leaflet-map
npm install
npm start
```

---

## Districts Covered

Dehradun, Haridwar, Nainital, Rishikesh, Almora, Uttarkashi, Tehri Garhwal, Chamoli, Rudraprayag, Pauri Garhwal, Pithoragarh, Bageshwar, Champawat, Udham Singh Nagar

---

## Contributing

Contributions are welcome. Open an issue or submit a pull request.

---



